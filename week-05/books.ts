let fullPrice: number = 8;
let twoBooks: number = (fullPrice * 2 ) * .95;
let threeBooks: number = (fullPrice * 3 ) * .9;
let fourBooks: number = (fullPrice * 4 ) * .8;
let fiveBooks: number = (fullPrice * 5 ) * .75;

function pricing(sorcerer: number, chamber: number, azkaban: number, goblet: number, phoenix: number ) {
  
}