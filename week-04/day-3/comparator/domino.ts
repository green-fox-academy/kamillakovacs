class Domino {
    values: number[];
    constructor(valueA: number, valueB: number) {
        this.values = [valueA, valueB];
    }

    compareTo(valueB: number) {
      if(this.values[0] < valueB)
    }
}

export {Domino};